<%= form_with(model: add_category_to_product) do |form| %>
  <% if add_category_to_product.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(add_category_to_product.errors.count, "error") %> prohibited this add_category_to_product from being saved:</h2>

      <ul>
        <% add_category_to_product.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :category_id, style: "display: block" %>
    <%= form.text_field :category_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
